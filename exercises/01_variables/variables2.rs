fn main() {
    // TODO: Change the line below to fix the compiler error.
    // You must initialise a variable before you can use it.
    // You don't *need* to specify the type here though.
    let x: i32 = 0;

    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
